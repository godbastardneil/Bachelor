const {Chat_user, Chat} = require('../models/models');
const sequelize = require('../database');
const ApiError = require('../error/ApiError');

class ChatController {
    async get(req, res) {
        
        
    }

    async set(req, res) {
        
        
    }
}

module.exports = new ChatController();
